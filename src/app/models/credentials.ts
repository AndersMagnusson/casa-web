export class Credentials {
    constructor(public username: string, public password: string) { }
}

export class DeviceCredentials {
    constructor(public serialNumber, public username: string, public password: string) { }
}
